rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Customer Photos: Public read for showcase, write allowed for authorized operations
    match /customer-photos/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }

    // Default deny all other paths
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
